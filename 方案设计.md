# 制造智能技术课程设计｜方案设计.md

## 1. 功能需求分析

### 1.1 用户角色

工业运维人员（系统使用者）：上传 / 导入设备传感器数据，查看设备故障诊断结果、寿命预测值，获取智能运维决策建议，查询历史设备运维记录。

### 1.2 功能性需求

1. **数据管理模块**

- 数据集导入：支持上传 NASA C‑MAPSS 格式 csv 时序数据；
- 数据清洗：缺失值填充、异常值过滤、特征工程，清洗后数据入库；
- 数据集浏览、历史数据查询。

2. **算法业务模块（三大核心技术）**

- 故障分类诊断：输入设备传感器特征，输出故障类型与故障置信度；
- 剩余寿命时序预测：输入时序监测序列，输出设备剩余使用寿命、风险等级（高 / 中 / 低）；
- 运维智能优化决策：结合故障类型、剩余寿命、设备重要度，输出维修优先级、备件建议、运维调度方案。

3. **前端展示模块**

- 系统首页；数据上传页面；故障诊断结果页面；寿命预测可视化图表；运维决策结果页面；历史记录查询页面。

4. **后端接口模块**
提供 REST API：数据上传接口、数据预处理接口、故障诊断推理接口、RUL 预测接口、运维决策接口、历史记录查询接口。
5. **数据库模块**
存储原始数据集、清洗后特征数据、模型推理输出结果、运维决策记录。

### 1.3 非功能需求

1. B/S 架构，浏览器即可访问 Demo；
2. 可完整跑通业务流程，支持本地运行；
3. 算法模块可独立调用；
4. 使用 Vibe Coding 流程开发，保留 prompt 日志；Git 小步提交；
5. 具备基础自动化测试，验证接口调用、数据读写、算法输出合理性。

## 2. 方案论证

工业设备运维是典型制造场景，设备故障停机将造成生产损失。传统运维多为定期检修，容易出现过维修或者维修不及时。
本系统采用**故障分类识别 + 时序剩余寿命预测 + 运维优化决策**组合方案：

1. 通过机器学习故障分类识别当前设备异常模式；
2. 通过时序预测评估设备还能安全运行多久；
3. 通过智能优化算法给出运维调度，把诊断‑预测‑决策完整闭环，贴合制造智能课程所学三大技术方向。
数据源采用公开 NASA C‑MAPSS 数据集，数据公开易得，不需要企业涉密数据，适合课程 Demo 开发。
对比只做单一故障识别的项目：本项目同时实现诊断、预测、决策三层智能能力，业务闭环完整，满足任务书至少 3 项课程技术要求。

## 3. 方案规划

### 3.1 技术栈选型

表格

| 层级 | 技术选型 |
| --- | --- |
| 前端 UI | Vue3 + Element Plus，简单 Web 页面，表单上传、表格展示、ECharts 绘制时序预测曲线 |
| 后端服务 | Python FastAPI，提供 RESTful API 接口 |
| 算法模块 | Scikit‑learn（故障分类）、Pytorch（时序 RUL 预测）、启发式优化算法实现运维调度 |
| 数据库 | MySQL，存储设备数据与推理结果 |
| 开发辅助 | Git 版本控制；AI 编码工具（通义灵码 / DeepSeek‑Code），Vibe Coding 开发模式； |
| 运行环境 | 本地 PC 环境，提供 README 说明部署步骤 |

### 3.2 需要配置的外部资源

1. NASA C‑MAPSS 公开数据集下载；
2. Python 依赖库环境；MySQL 本地数据库；
3. 大模型代码工具，用于 Vibe Coding 开发；
4. 无付费资源，全部开源公开资源。

### 3.3 数据集来源

NASA C‑MAPSS 航空发动机退化数据集，公开工业时序数据集，魔搭 ModelScope、飞桨 AI‑Studio 均可下载。
数据集包含多台发动机运行传感器读数、工况、故障退化标签、真实剩余寿命标签，用于训练、测试算法模块。开发 Demo 阶段：一部分数据用于模拟训练，一部分样本作为测试样本用于系统演示。

### 3.4 前端方案

页面模块：

1. 登录 / 首页；
2. 数据上传页面：上传 csv 数据集文件；
3. 数据集管理页面：查看已入库数据；
4. 智能诊断页面：选中设备样本，触发故障分类，输出故障类型、置信度；
5. 寿命预测页面：时序曲线可视化，输出 RUL 预测值，风险等级；
6. 运维决策页面：输出维修优先级、备件建议、运维方案；
7. 历史记录页面：查询全部历史推理结果。

### 3.5 后端 & 算法方案

后端 FastAPI 接收前端 http 请求，拆分多个业务接口；
算法模块拆分为独立子模块：

1. **故障分类模块（智能故障诊断）**：对传感器特征做特征工程，训练分类模型，输入样本特征，输出故障类别与置信分数。
2. **RUL 剩余寿命预测模块（时序预测）**：时序模型输入传感器时间序列，输出剩余使用寿命预测结果。
3. **运维优化决策模块（智能优化决策）**：接收故障结果、RUL 值、设备权重，用优化算法输出运维排程建议。

后端完成：文件接收、数据预处理、调用算法模块、结果写入 MySQL 数据库，返回 JSON 结果给前端渲染。

### 3.6 数据库简要设计

主要数据表：

1. `device_raw_data`：设备原始传感器数据表
2. `device_clean_data`：清洗完成特征数据集表
3. `fault_diag_result`：故障诊断推理结果
4. `rul_predict_result`：剩余寿命预测结果
5. `maintenance_decision`：运维决策输出记录

## 4. 技术路线

1. 阶段 1（今明两天 D1‑D5）：完成选题说明.md、方案设计.md，提交 Git 仓库，完成选题打卡；确认数据集，梳理项目全部需求规格。
2. 阶段 2（D6）：数据集下载、清洗、特征处理，导入 MySQL 数据库；编写数据处理脚本。
3. 阶段 3（D7‑D8）：Vibe Coding 模式开发：后端 API 开发、三大算法模块编码调试；前端页面开发；前后端联调；Git 小步提交；留存 prompt 日志。
4. 阶段 4（D9）：系统集成调试、自动化测试；完善 README；撰写课程设计报告；录制 3 分钟演示视频。
5. 阶段 5（D10）：制作答辩 PPT，参加答辩；完整提交全部交付物。

## 5. 详细计划安排

表格

| 时间 | 工作内容 | 产出物 |
| --- | --- | --- |
| D1‑D5（本周四 24 点前） | 选题调研，撰写选题说明.md、方案设计.md，推送 Git 仓库，选题接龙打卡 | 选题说明.md、方案设计.md，Git 提交记录 |
| D6 | 数据集获取、清洗、特征工程，数据入库 | 数据集脚本，数据库完成建表与数据导入 |
| D7‑D8 | 后端接口开发；三大算法模块实现；前端页面开发；前后端对接调试；Git 持续提交，保存 prompt 日志 | 可运行 Demo 雏形，代码仓库 |
| D9 | 系统集成测试；完善 Readme；撰写完整设计报告；录制 3 分钟演示视频 | 完整可运行 Demo，设计报告，演示视频，过程档案 |
| D10 | 答辩 PPT 制作；答辩；提交全部成果 | PPT，全套提交物 |

## 6. 参考文献

[1] 张智海。制造智能技术基础 [M]. 北京：清华大学出版社，2022.
[2] 杨杰。人工智能基础 [M]. 北京：机械工业出版社，2025.
[3] Saxena A, Goebel K. C‑MAPSS Turbofan Engine Degradation Simulation Data Set [DB/OL]. NASA Ames Research Center.
[4] 周志华。机器学习 [M]. 北京：清华大学出版社，2016.
